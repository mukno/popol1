<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.mapper.BoardMapper">

	<insert id="writeAction">
		<!-- 
		<selectKey keyProperty="updatedate" order="BEFORE" resultType="String">
			select to_char(sysdate,'yyyymmdd hh24:mi:ss') from dual;
		</selectKey> -->
		insert into board(bno,title,writer,updatedate,board_content) 
		values(seq_board.nextval,#{title},#{writer},sysdate,#{board_content})
		
	
	</insert>


</mapper>